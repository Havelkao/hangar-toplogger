{
    "version": 2,
    "builds": [
        {
            "src": "server/main.py",
            "use": "@vercel/python"
        },
        {
            "src": "server/static/**",
            "use": "@vercel/static"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "server/main.py"
        }
    ]
}
